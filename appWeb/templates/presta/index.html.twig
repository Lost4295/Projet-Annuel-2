{% extends 'base.html.twig' %}
{% block body %}


	<div class="row py-2 d-flex bg-orange mb-5">
        <div class="fs-1 text-center ">
        Vous pouvez faire votre demande de devis ici
        </div>	
	</div>
	
	<div class="row">
	{% for presta in prestas %}
		<a href="{{ path('cacaporut', {'id': presta.id}) }}" class="col-md-4 py-1 text-decoration-none">
			<div class="card">
			<div class="card-body">
				<img src={{asset("images/presta/menage.jpg")}} class="card-img-top" alt="...">
				<h5>{{presta.societyName|slice(0,30)}}</h5>
			</div>
			</div>
		</a>
    {% endfor %}
			
	<div class="row d-flex justify-content-center mt-5">
        <div class="fs-3 text-center col-6 bg-info rounded pb-2 pt-2">
        Voir les autres prestataires disponibles en cliquant ici
        </div>	
	</div>
{% endblock %}
