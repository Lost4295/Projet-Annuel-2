{% extends 'base.html.twig' %}

{% block title %}
	{{"reset"|trans }}
{% endblock %}

{% block body %}
	<h1>{{"reset"|trans }}</h1>
	<div class="card bg-info">
		<div class="card-body">
			{{ form_start(resetForm) }}
			{{ form_row(resetForm.plainPassword) }}
			<button class="btn btn-primary">{{"resetpwd"|trans}}</button>
			{{ form_end(resetForm) }}
		</div>
	</div>
{% endblock %}
