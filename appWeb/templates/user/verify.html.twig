{% extends 'base.html.twig' %}
{% block body %}
    <h1>Verification de l'inscription</h1>

    {{ form_start(form) }}
    {{ form_row(form.justification) }}
    <button type="submit" class="btn btn-primary">Valider</button>
    {{ form_end(form) }}
{% endblock %}